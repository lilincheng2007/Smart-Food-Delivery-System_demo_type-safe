# 2026-06-22

- 为 `Type-safe_project` 制定新的可维护性整改计划，保存为 `PLAN/MAINTAINABILITY_REMEDIATION_PLAN.md`。计划仿照既有 `PLAN/OPTIMIZATION_PLAN.md` 结构，覆盖 DTO 落位、图片上传复用、退款 workflow、评价提交流程、结算提交服务化、AI API 降厚、routes JSON codec 自动派生收敛和文档/审计同步；进展记录区域保持空白。
- 按整改计划完成第一批：将 `PlatformPromotionsResponse` 迁入 `frontend/src/objects/admin/apiTypes/`，并让订单/退款/评价图片上传 API 统一复用 `@/lib/local-image-file` 的 `fileToBase64`；已通过 `npm run typecheck --prefix frontend`，并在计划文档回填进展与第二批建议。
- 按批次继续完成第二至第四批：新增 `RefundWorkflowService.rejectByAdmin/rejectByMerchant` 并收敛管理员/商家退款驳回 API；新增 `ReviewSubmissionService` 并将顾客评价 API 收敛为薄入口；新增 `CheckoutCommandService` 并将 `CheckoutAPIMessage` 改为命令服务入口。相关改动已通过后端编译与前端 typecheck，并在计划文档更新到“下一批建议（第五批）”。
- 在 `README.md` 靠前位置新增并精简“小组分工情况”章节，按架构、业务端、AI 能力和协作方式整理李/张分工，并同步调整后续章节编号。
- 按整改计划继续完成第五至第七批：新增 `merchant/services/CatalogQueryService` 与 `ai/services`（`AISearchService`、`AIDietWeeklyReportService`、`AIMerchantBusinessSuggestionsService`、`AIOrderProgressNarrativesService`、`AIReviewSummaryService`），将 5 个 AI API 收敛为薄入口并移除 AI 模块内 API 调 API；`AIRoutes` 去除 `generic.auto` 且补齐 `AIJsonCodecs`；同步更新计划文档进展、`DIRECTORY_LAYERING_GUIDE.md`、`README.full.md`、`API_INVENTORY.md` 与两类审计脚本。验证结果：`cd backend && sbt -batch compile` 通过，类型安全审计 `48 PASS / 0 FAIL`，可维护性审计 `22 PASS / 1 WARN / 0 FAIL`（剩余告警为非 AI routes 的 `generic.auto` 渐进迁移）。
- 完成第六批阶段二收尾：为 `admin/merchant/order/review/rider/user` 的模块 JsonCodecs 补齐 APIMessage codec，并从所有 `*Routes.scala` 移除 `io.circe.generic.auto.*`；将可维护性和类型安全审计中的 routes 自动派生检查升级为全模块检查/失败级别；更新 `PLAN/MAINTAINABILITY_REMEDIATION_PLAN.md`、`DIRECTORY_LAYERING_GUIDE.md`、`README.full.md`。验证结果：`cd backend && sbt -batch compile` 通过，类型安全审计 `48 PASS / 0 FAIL`，可维护性审计 `23 PASS / 0 WARN / 0 FAIL`。
